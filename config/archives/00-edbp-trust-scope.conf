// live-build stages *.key.{chroot,binary} under trusted.gpg.d. Keep those
// files usable only through explicit Signed-By paths; vendor keys are never
// globally eligible.
Dir::Etc::Trusted "/usr/share/keyrings/debian-archive-keyring.gpg";
Dir::Etc::TrustedParts "/etc/apt/keyrings";
