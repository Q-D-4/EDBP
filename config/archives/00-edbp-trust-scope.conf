// live-build stages unsuffixed *.key inputs for both archive passes. Keep
// those files usable only through explicit Signed-By paths; vendor keys are
// never globally eligible.
Dir::Etc::Trusted "/usr/share/keyrings/debian-archive-keyring.gpg";
Dir::Etc::TrustedParts "/etc/apt/keyrings";
