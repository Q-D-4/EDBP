// live-build stages *.key.chroot under trusted.gpg.d. Keep those files usable
// only through an explicit Signed-By path; do not make vendor keys global.
Dir::Etc::Trusted "/usr/share/keyrings/debian-archive-keyring.gpg";
Dir::Etc::TrustedParts "/etc/apt/keyrings";
