# SPDX-License-Identifier: GPL-2.0-or-later
# Derived in part from the USBGuard 1.1.3 upstream daemon configuration.
# See THIRD_PARTY_NOTICES.md for provenance and attribution details.

RuleFile=/etc/usbguard/rules.conf
ImplicitPolicyTarget=block
PresentDevicePolicy=apply-policy
PresentControllerPolicy=keep
InsertedDevicePolicy=apply-policy
AuthorizedDefault=none
RestoreControllerDeviceState=false
DeviceManagerBackend=uevent

# Root retains full IPC access. The sudo group receives a narrower ACL through
# IPCAccessControlFiles; do not use Debian's broad plugdev default.
IPCAllowedUsers=root
IPCAccessControlFiles=/etc/usbguard/IPCAccessControl.d/
DeviceRulesWithPort=false

AuditBackend=FileAudit
AuditFilePath=/var/log/usbguard/usbguard-audit.log
HidePII=false