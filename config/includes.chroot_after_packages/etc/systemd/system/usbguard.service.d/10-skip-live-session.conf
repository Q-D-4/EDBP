[Unit]
# A USB-booted Live session depends on its class-08 medium for the SquashFS
# root. Enforcing the installed-system policy there would block the boot medium.
# Debian live-boot mounts that medium at /run/live/medium before systemd starts.
ConditionPathExists=!/run/live/medium
