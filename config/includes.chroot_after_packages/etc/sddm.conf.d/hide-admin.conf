# Managed by EDBP. This hides the break-glass administrator from the chooser;
# it is not an authorization control and does not disable typed-name login.
[Users]
HideUsers=localadmin
RememberLastUser=false
